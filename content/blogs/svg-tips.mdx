---
title: SVG的使用
date: 2024-1-21
---

想让 svg 图片的颜色跟随父元素的 `color` 值变化，可先用编辑器打开 svg 图片文件，修改以下 2 点后保存即可：

在 `svg` 标签中加上 `fill="currentColor"`
在 `path` 标签中去掉 `fill="..."`，或者改为 `fill="currentColor"`

拿到的 svg 图片如果是自带颜色的，一般父元素的颜色设置就会无效，那么就需要修改以上 2 点。
